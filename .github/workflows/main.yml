name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven   
    - name: Build with Maven
      run: cd my-app
    - run: mvn -B package --file ./my-app/pom.xml
    - run: cd ./my-app && mkdir staging && cp target/*.jar staging
    - uses: actions/upload-artifact@v3
      with:
       name: Package
       path: ./my-app/staging
       
    - name: Download a Build Artifact
      uses: actions/download-artifact@v3.0.2
      with:
       name: Package
       path: ./my-app
       
    - uses: azure/docker-login@v1
      with:
       login-server: myregistry990.azurecr.io
       username: myregistry990
       password: mnbX213OaY5bTYPQazLqLjg5aT+CvFfIhufL9lYY+n+ACRBvU9sZ
    - run: |
       sudo docker build ./my-app -t myregistry990.azurecr.io/k8sdemo:v1
       sudo docker push myregistry990.azurecr.io/k8sdemo:v1
       
       
       
